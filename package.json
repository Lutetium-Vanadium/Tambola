{
  "name": "Tambola",
  "version": "1.0.0",
  "main": "build/server.js",
  "license": "MIT",
  "scripts": {
    "watch": "nodemon --config \"./utils/nodemon.json\"/",
    "start": "node ./build/server.js",
    "run:frontend": "cd Frontend && yarn watch",
    "build:frontend": "cd Frontend && yarn build",
    "build:backend": "tsc",
    "build": "yarn build:frontend && yarn build:backend"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^13.7.0",
    "@types/socket.io": "^2.1.4",
    "nodemon": "^2.0.2",
    "ts-node": "^8.6.2",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  }
}
